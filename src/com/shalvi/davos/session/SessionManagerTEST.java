package com.shalvi.davos.session;

import junit.framework.TestCase;

public class SessionManagerTEST extends TestCase {

    public void testNewSession() {
        /*
        Session s = SessionManager.newSession();
        assertNotNull(s);
        assertTrue(s instanceof Session);
        */
    }
    
    public void testGetSession() {
        /*
        Session s1 = SessionManager.newSession();
        Session s2 = SessionManager.newSession();
        
        assertTrue(s1.equals(s2));
        assertFalse(s1 == s2);
        
        
        int id1 = s1.getId(), id2 = s2.getId();
        
        */
    }
    
    public void testStoreSession() {
        
    }
}
